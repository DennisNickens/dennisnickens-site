{
  "name": "sr-blueprint-generator",
  "version": "1.0.0",
  "description": "Spiritual Romeo Alignment Blueprint generator. Receives webhook from GHL, scores assessment answers, calls Claude API to generate personalized Blueprint, saves to Vercel Blob, updates GHL contact.",
  "main": "api/generate-blueprint.js",
  "type": "commonjs",
  "scripts": {
    "test": "node test/test-scoring.js",
    "regenerate-question-map": "node scripts/regenerate-question-map.js"
  },
  "dependencies": {
    "@vercel/blob": "^0.23.0",
    "@vercel/functions": "^1.4.0",
    "marked": "^12.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
